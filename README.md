# @oneworks/mobile-runtime

Platform-neutral JavaScript runtime primitives for One Works mobile hosts. This repository currently contains only the platform-neutral core. Host adapters have not yet been migrated.

## Development

```sh
pnpm install
pnpm typecheck
pnpm build
pnpm test
pnpm lint
pnpm format:check
```
